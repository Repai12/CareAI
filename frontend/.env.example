# Copy this file to frontend/.env.local and adjust if needed.
# Never commit frontend/.env.local - it's already in .gitignore.

# Base URL of the FastAPI backend. Defaults to http://localhost:8000 if unset,
# so this file is only needed if your backend runs on a different host/port.
NEXT_PUBLIC_API_URL=http://localhost:8000
